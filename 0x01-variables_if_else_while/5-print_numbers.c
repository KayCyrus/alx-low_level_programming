#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* betty style doc function main goes there */
/**
 * main - entry point
 * Return: 0 (success)
 */
int main(void)
{
	int x = 0;

	while (x <= 9)
	{
		printf("%d", x);
		x += 1
	}
	putchar(10);
	return (0);
}
